# Supabase project settings — get these from https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — server-only, NEVER exposed to the client or committed.
# Used only in trusted server contexts (e.g. scheduled jobs in later phases).
SUPABASE_SERVICE_ROLE_KEY=
